/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['letsenhance.io', 'img.freepik.com', 'encrypted-tbn0.gstatic.com'], // Add the external hostname here
    },
}

export default nextConfig;
