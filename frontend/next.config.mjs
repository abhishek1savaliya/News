/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['letsenhance.io', 'burst.shopifycdn.com', 'st2.depositphotos.com', 'example.com', 'img.freepik.com', 'encrypted-tbn0.gstatic.com'], // Add the external hostname here
    },
}


export default nextConfig;
